# ── Cherry Studio MCP Servers 依赖 ──

# 核心 (所有服务器必需)
mcp>=1.0.0

# PDF Server
pymupdf>=1.23.0          # PDF 读取
reportlab>=4.0            # PDF 生成

# Office Server
python-docx>=1.1.0        # Word .docx
openpyxl>=3.1.0           # Excel .xlsx
python-pptx>=0.6.23       # PowerPoint .pptx

# Image Server
Pillow>=10.0.0            # 图片处理
pytesseract>=0.3.10       # OCR (还需系统安装 Tesseract)
